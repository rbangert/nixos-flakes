"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkTitle = void 0;
const regex_1 = require("../regex");
const Types_1 = require("../Types");
function checkTitle(line, i, lengthAtIndex, context) {
    const titleMatch = line.match(regex_1.TITLE_REGEX);
    if (titleMatch) {
        context.title = titleMatch[2].trim();
        const titleTagIndex = line.indexOf(titleMatch[1]);
        context.ranges.push({
            type: Types_1.RangeType.Title,
            from: lengthAtIndex[i] + titleTagIndex,
            to: lengthAtIndex[i] + titleTagIndex + titleMatch[1].length,
            lineFrom: {
                line: i,
                index: titleTagIndex,
            },
            lineTo: {
                line: i,
                index: titleTagIndex + titleMatch[1].length,
            },
        });
        return true;
    }
    return false;
}
exports.checkTitle = checkTitle;
